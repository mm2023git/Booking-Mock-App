# Booking-Mock-App
Setup directions:

Backend:
1. Enter main directory
2. python -m venv venv
3. pip install -r requirements.txt
4. To run use flask run

Frontend:
1. Enter /frontend/
2. npm install
3. npm start
